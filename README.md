# AI ChatBot

A small local web app for the Ollama `llama3` model.

## Run

```bash
python3 app.py
```

Open http://127.0.0.1:8000 in your browser.

Make sure Ollama is running and `llama3` is installed.
